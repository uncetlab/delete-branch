name: "Create Branch"
description: "Create a Branch"
input:
    branch:
        description: "branch to delete"
    github_token :
        description: "access token"
        required: true

runs: 
    using: 'docker'
    image: 'Dockerfile'
